{
  "name": "take-a-selfie",
  "version": "0.0.1",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html",
    "build": "parcel build index.html --public-url /demo/selfie -d dist/selfie",
    "deploy": "scp -r dist/selfie/ root@xxx.xxx:/root/blog/demo"
  },
  "author": "imgss",
  "license": "MIT",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "stylus": "^0.54.7"
  },
  "eslintConfig": {
    "env": {
      "browser": true,
      "es6": true
    },
    "parserOptions": {
      "sourceType": "module"
    },
    "plugins": [],
    "rules": {
      "semi": "error"
    }
  },
  "dependencies": {
    "hammerjs": "^2.0.8"
  }
}
